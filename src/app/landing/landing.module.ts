import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * @todo - Landing module is the init one, eg entry point. (Production)
 *         App module is the constructor (weird flex but ok)
 */

@NgModule({
  declarations: [],
  imports: [
    CommonModule
  ]
})
export class LandingModule { }
